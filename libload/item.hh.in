#include <vector>
#include <string>
#include <climits>
#include <iostream>
#define __DEBUG_MODE__ @DEBUG_MODE@
#if __DEBUG_MODE__==0
#undef DEBUG_MODE
#endif

class Item {
    public:
       Item(std::string* name, int runtime);
       void SubtractTime(int time);
       bool IsDone();
       std::string GetName();
       int GetRunTime();
    private:
       std::string Name;
       int RunTime;
       int TimeRemaining;
};
